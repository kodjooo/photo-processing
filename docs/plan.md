План реализации MVP сервиса автоматической обработки фотографий

Статус плана:
- Все этапы ниже пока не выполнены.
- После завершения каждого этапа нужно дописывать рядом пометку `выполнено`, не удаляя сам этап.

Этап 1. Уточнение и фиксация проектной структуры — `выполнено`
- Зафиксировать MVP-границы: только ZIP на вход, только публичные ссылки Яндекс Диска, только Docker Desktop как способ запуска.
- Уточнить и задокументировать состав сервисов: Telegram Bot, API / Job Manager, Redis, Worker, PostgreSQL, временное файловое хранилище.
- Описать ответственность каждого компонента и точки интеграции в `docs/architecture.md`.
- Согласовать единый формат идентификатора задачи `job_id`, жизненный цикл задачи и таблицу статусов.
- Определить конфигурационные параметры проекта и подготовить список обязательных переменных окружения для `.env.example` и `.env`.

Этап 2. Базовая контейнеризация и оркестрация через Docker Desktop — `выполнено`
- Подготовить единый `docker-compose.yml` для локального запуска всех компонентов проекта только через Docker Desktop.
- Добавить контейнеры для API / Bot, Worker, Redis, PostgreSQL и при необходимости отдельный контейнер для миграций.
- Настроить общие volume для временных файлов задач и отдельные volume для данных PostgreSQL/Redis.
- Настроить внутреннюю сеть Docker Compose, healthcheck и корректный порядок старта зависимостей.
- Добавить команды запуска, пересборки и остановки контейнеров в `README.md`.
- Проверить, что проект поднимается одной командой без использования локального Python-окружения.

Этап 3. Каркас приложения и конфигурация — `выполнено`
- Выбрать и зафиксировать основной стек Python-бэкенда и Telegram-бота.
- Реализовать модуль конфигурации, который читает параметры только из переменных окружения.
- Подготовить базовую структуру каталогов приложения: bot, api, workers, services, repositories, processing, schemas, core.
- Подключить централизованное логирование, пригодное для чтения из Docker logs.
- Добавить базовые проверки конфигурации на старте контейнеров.

Этап 4. Слой хранения данных и модель задач — `выполнено`
- Спроектировать схему базы данных для хранения задач, статусов, отчетов и технических логов.
- Создать таблицы для задач, событий статуса, результатов обработки и отмены задач.
- Подготовить миграции базы данных и автоматический запуск миграций в Docker-сценарии.
- Реализовать repository/service слой для создания задачи, смены статуса, записи ошибок, итоговой ссылки и статистики по файлам.
- Учесть запросы Telegram-бота: последняя задача пользователя, статус по `job_id`, отмена задачи.

Этап 5. API / Job Manager — `выполнено`
- Реализовать backend API или внутренний сервисный слой для создания и управления задачами.
- Добавить валидацию входных данных: ссылка, режим обработки, пользователь Telegram, допустимые лимиты.
- Реализовать постановку задачи в очередь Redis.
- Реализовать команды получения статуса, отмены задачи и чтения последнего результата пользователя.
- Подготовить единый формат ответов и ошибок для бота и внутренних сервисов.

Этап 6. Telegram-бот — `выполнено`
- Реализовать команды `/start`, `/help`, `/process`, `/status`, `/presets`, `/logo`, `/cancel`, `/last`.
- Поддержать запуск обработки как по команде, так и по простой отправке ссылки.
- Добавить валидацию пользовательского ввода и понятные сообщения об ошибках.
- Реализовать отправку пользователю `job_id`, текущего статуса, итоговой ссылки и краткого отчета.
- Предусмотреть сценарии таймаута, отмены и недоступности внешних сервисов.

Этап 7. Интеграция с Яндекс Диском — `выполнено`
- Реализовать проверку, что ссылка относится к Яндекс Диску и является публичной.
- Реализовать получение метаданных удаленного файла до скачивания: тип, размер, имя.
- Проверять, что по ссылке доступен ZIP-архив и что его размер не превышает лимит.
- Реализовать потоковое скачивание архива во временную директорию задачи.
- Реализовать загрузку результата обратно на Яндекс Диск: как минимум ZIP-архива, при возможности предусмотреть расширение до загрузки папки.
- Продумать обработку сетевых ошибок, истекших ссылок, ошибок доступа и повторных попыток.

Этап 8. Подготовка файлового пайплайна задачи — `выполнено`
- Реализовать создание рабочих директорий задачи по шаблону `/tmp/jobs/{job_id}/...` внутри контейнеров.
- Реализовать безопасную распаковку ZIP без выхода за пределы рабочей директории.
- Реализовать сканирование распакованного содержимого с сохранением вложенной структуры папок.
- Отфильтровать поддерживаемые и неподдерживаемые файлы, собрать предварительную статистику.
- Проверять лимиты по числу файлов и изображений до запуска тяжелой обработки.
- Подготовить очистку временных файлов после завершения или отмены задачи.

Этап 9. Реализация пайплайна обработки изображений — `выполнено`
- Реализовать чтение изображений по одному файлу или малыми партиями без загрузки всего архива в память.
- Реализовать нормализацию: EXIF-ориентация, цветовое пространство, базовая валидация файла.
- Реализовать расчет метрик изображения: яркость, контраст, шум, резкость, цветовой сдвиг.
- Реализовать аккуратное адаптивное улучшение изображения классическими фильтрами без генеративных методов.
- Подготовить систему пресетов обработки, доступных через бот.
- Обеспечить конфигурируемую параллельность обработки внутри одной задачи.

Этап 10. Наложение логотипов — `выполнено`
- Определить требования к входным логотипам: формат, прозрачность, размеры, расположение.
- Реализовать загрузку логотипов из конфигурации или выделенной директории внутри контейнера.
- Реализовать масштабирование логотипов относительно размеров изображения.
- Добавить наложение двух полупрозрачных логотипов в нижний левый и правый углы.
- Предусмотреть защиту от выхода логотипа за границы изображения и деградации качества.

Этап 11. Формирование результата и отчетности — `выполнено`
- Реализовать сохранение обработанных файлов с сохранением структуры каталогов.
- Реализовать упаковку результата в ZIP как основной формат MVP-выгрузки.
- Сформировать итоговый отчет по задаче: всего найдено, обработано, пропущено, причины пропуска.
- Сохранить отчет в базе и подготовить краткую текстовую версию для Telegram-бота.
- Сохранить итоговую ссылку на результат и время завершения задачи.

Этап 12. Очередь, воркер и управление выполнением — `выполнено`
- Реализовать worker-процесс, который читает задачи из Redis и последовательно проходит все статусы обработки.
- Настроить ограничение параллельности по числу одновременно обрабатываемых задач.
- Реализовать безопасную отмену задачи на этапах до финальной выгрузки.
- Добавить механизм heartbeat или периодического обновления статуса для длинных задач.
- Предусмотреть повторяемость операций там, где это безопасно, и защиту от двойного запуска одной задачи.

Этап 13. Обработка ошибок и наблюдаемость — `выполнено`
- Реализовать единый словарь пользовательских ошибок и технических ошибок.
- Настроить подробные структурированные логи по `job_id`.
- Добавить диагностику по этапам: скачивание, распаковка, обработка, упаковка, загрузка.
- Подготовить базовые метрики или хотя бы счетчики в логах для времени этапов и числа файлов.
- Проверить, что ошибки внешних сервисов не приводят к зависшим задачам.

Этап 14. Тестирование — `выполнено`
- Подготовить тестовую стратегию: unit, integration, smoke.
- Добавить unit-тесты для валидации ссылок, статусов задач, лимитов, фильтрации файлов и бизнес-логики отчетов.
- Добавить integration-тесты для очереди, базы данных, потокового скачивания и worker-пайплайна.
- Добавить тесты обработки изображений на типовых кейсах: JPEG, PNG, WEBP, поврежденные файлы, неподдерживаемые форматы.
- Добавить Docker-based smoke test, который поднимает сервисы и проверяет базовый сценарий создания задачи.
- Актуализировать тесты в папке `tests` по мере реализации каждого компонента.

Этап 15. Конфигурация и эксплуатационная документация — `выполнено`
- Актуализировать `.env.example` полным списком обязательных переменных.
- Заполнить `.env` комментариями, где получить каждый ключ и для какого доступа он нужен.
- Проверить, что `.env` исключен из Git и не отправляется в GitHub.
- Добавить в `README.md` только необходимую информацию по запуску, остановке, пересборке и деплою на удаленный сервер через Docker.
- Указать в `README.md` требования к Docker Desktop, порядок первого запуска и базовые команды диагностики.

Этап 16. Инициализация git и подготовка публикации репозитория — `выполнено`
- Проверить наличие git-репозитория и при отсутствии инициализировать его.
- Настроить `.gitignore` для Python, Docker, временных файлов задач, кэшей, логов и `.env`.
- Проверить корректность remote для GitHub-репозитория `https://github.com/kodjooo/photo-processing.git`.
- Подготовить проект к пушу после завершения реализации и прохождения проверок.

Этап 17. Финальная приемка MVP
- Проверить полный пользовательский сценарий: ссылка из Telegram -> очередь -> обработка -> загрузка результата -> ответ пользователю.
- Проверить граничные кейсы: невалидная ссылка, слишком большой архив, слишком много файлов, неподдерживаемые форматы, отмена задачи.
- Проверить устойчивость запуска и перезапуска через Docker Desktop.
- Сверить фактическую реализацию с `docs/requirements.md`, `docs/arch-rules.md` и `docs/architecture.md`.
- После завершения каждого этапа обязательно отметить его в этом документе пометкой `выполнено`.

Этап 18. Поддержка RAW-форматов `.CR2` и `.ARW` — `выполнено`
- Добавить поддержку RAW-файлов `.CR2` и `.ARW` в файловый пайплайн.
- Подключить библиотеку декодирования RAW, совместимую с Docker-образом проекта.
- Реализовать конвертацию RAW в рабочее RGB-изображение перед стандартным этапом улучшения.
- Обновить тесты, документацию и архитектуру с учетом новых форматов.
